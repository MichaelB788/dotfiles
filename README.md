Dotfiles for Void and Fedora Linux, whichever I decide to use.

Installation under Void is as simple as running the script on a fresh install, 
and running the services noted in `void.md`.

Installation under Fedora assumes Fedora Workstation is installed. Also,
activation of the Terra repo will be necessary to install some packages.
