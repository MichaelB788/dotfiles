Dotfiles for Void and Fedora Linux.

Installation under Void is as simple as running the script on a fresh install, 
and running the services noted in `void.md`.

Installation under Fedora assumes a Fedora DE is installed.

The Terra repo will be necessary to install some packages.
